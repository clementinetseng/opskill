export function AgentInstruction({ children }: { children: React.ReactNode }) {
    // Humans don't need to see this.
    // In the future, we can add a toggle to "Debug Mode" to show these.
    return null
}
